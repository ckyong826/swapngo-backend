DB_HOST=localhost
DB_USER=root
DB_PASSWORD=secretpassword
DB_NAME=swapngo
# Matches docker-compose host mapping (5433 -> container 5432). Use DB_PORT=5432 if you use a local Postgres on the default port.
DB_PORT=5433
